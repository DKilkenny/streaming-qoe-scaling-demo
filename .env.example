# OpenTelemetry tracing (metrics always on via Prometheus).
# Set to true and point at an OTLP collector (or Grafana Cloud / Datadog agent) to emit traces.
OTEL_ENABLED=false
OTEL_EXPORTER_OTLP_ENDPOINT=
